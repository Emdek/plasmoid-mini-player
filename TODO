- add track properties dialog;
- try to guess title and artist using "-" separator;
- use TagLib to fetch and set meta data (?);
- add playlist statusbar (total length, current time etc.) (?);
- dynamic playlist with most often played tracks (?);
- add audio analyzer (?);
- visualizations for audio files (?);
- add music library functionality (?);
- Last.fm scrobbler support (?);
- load videos from YouTube etc. (?);
- alarm clock like functionality - play selected playlist on selected hour or every hour etc. (?).
